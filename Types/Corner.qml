import Quickshell
import QtQuick
import QtQuick.Shapes

PanelWindow {
    id: root

    anchors {
        top: true
    }
    exclusiveZone: 0


    color: "transparent"
    //add in code to make a concave corner between the main bar and the media/dashboard tab
    //git commit -m "added more dashboard capabilities.
    //dashboard now holds both the media tab and the power profiles daimons"

    

    
}